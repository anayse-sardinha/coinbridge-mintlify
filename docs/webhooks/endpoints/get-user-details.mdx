---
title: "Get User Eligibility"
api: "GET /coinbridge/api/v1/user/{userId}/details"
description: "Validates user eligibility and retrieves profile status during onboarding."
layout: "regular"
---

This endpoint is an optional step in the user authentication flow. It is triggered by the **CoinBridge Backend** immediately after an `Authentication` request is initiated by the mobile app. 

Its primary purpose is to verify if the user exists and meets the specific eligibility criteria (such as Know Your Customer (KYC) requirements or country-specific regulations) defined by your application's logic back-end.

<Note> It is strongly recommended to check eligibility before introducing the service to the user to prevent frustration if they are found to be ineligible. </Note>

## User Eligibility Flow
This flow illustrates the end-to-end authentication handshake required to verify user status across back-end systems.

<Note>
### Prerequisites:
To ensure a successful eligibility check, the CoinBridge SDK must be [initialized](/docs/sdk/installation/initialize) before you call the eligibility method.
</Note>

![User Eligibility Flow](/images/end-user-details.jpg)

The following steps break down the sequential logic of the validation process:

<Steps>
  <Step title="Login Request">
    The **Application** sends a `Login` request to the **Application Back-End**.
  </Step>
  <Step title="User Identification">
    The **Application Back-End** identifies the user and sends a `UserId` back to the **Application**.
  </Step>
  <Step title="SDK Authentication">
    The **Application** asks the **CoinBridge SDK** to `Authenticate (with userId)`.
  </Step>
  <Step title="Backend Request">
    The **CoinBridge SDK** forwards the `Authentication` request to the **CoinBridge Backend**.
  </Step>
  <Step title="User Verification">
    The **CoinBridge Backend** reaches out to the **Application Back-End** to verify the user is **Valid** or **Invalid**.
  </Step>
  <Step title="Authentication Result">
    The **CoinBridge Backend** sends the result back through the **CoinBridge SDK** to the **Application**.
  </Step>
</Steps>

<Note>
  If your application needs to supply additional data to enable specific services, use this endpoint to transmit that information. Please consult the **CoinBridge Product Team** before modifying the response schema.
</Note>

### Path Parameters

<ResponseField name="valid" type="boolean" required>
  Validates that the userId exists. Controls whether the onboarding process can proceed.
</ResponseField>

<ResponseField name="personalId" type="string">
  Personal identification number for the given `userId`.
</ResponseField>

<ResponseField name="countryOfResidence" type="string">
  The ISO-31661 country code representing the country of residence for a given `userId`.
</ResponseField>

<ResponseExample>

```json Response
{
  "valid": true
}
```
</ResponseExample>

See more about this process:

<CardGroup cols={1}>
  <Card title="Check User Eligibility" icon="mobile" href="/docs/sdk/installation/user-eligibility">
    Verify user eligibility for card issuance and services to ensure regulatory compliance.
  </Card>
</CardGroup>
